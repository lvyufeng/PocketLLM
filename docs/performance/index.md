# Performance

Measured behaviour, reported separately for prefill and decode. Every page here is
a run record: hardware, checkpoint, commit, and invocation are stated with the
numbers. Read [Benchmarking and reporting rules](../guides/benchmarking.md) first
if you intend to compare any of them, and do not carry a decode figure to a
different generation length — a `TG4` measurement says nothing about `TG512`.

| Document | What it records |
| --- | --- |
| [DSpark speculative decoding with adaptive draft-length gating](dspark.md) | The DeepSeek-V4-Flash draft module, why the fixed accept threshold was falsified, and the online-calibrated gating that replaced it. |
| [FlashMemory 1M context](flashmemory_1m_context.md) | The FlashMemory + KV_SWAP design for long-context memory reduction, and its current implementation status. *(Chinese)* |
| [MiniMax-M2 decode bottleneck analysis](minimax_decode_bottleneck_analysis.md) | Per-phase profiling of MiniMax-M2 decode and prefill on TP4, and which phases the optimizations then targeted. |
| [Qwen4-Exp heterogeneous TP4 performance](qwen4_exp_performance.md) | Moving Qwen4-Exp experts to host memory: measured prefill gain, and why the real floor was the disk rather than PCIe. |
| [CPU offload and prefetch: the measured ceiling](cpu_offload_profile.md) | Layer-granularity offload measured rather than planned: the H2D and overlap ceilings, the storage rate behind them, and why #156's decode and prefetch targets are unreachable on this host. |
| [Qwen quantized KV cache at 65K (TG512)](qwen_kv_cache_65k_tg512.md) | The authoritative quantized-KV table at 65K/TG512, including corrections to two earlier throughput claims. |
| [Qwen drafter acceptance and speedup](qwen_drafter_acceptance.md) | The three-way head-to-head of MTP, DSpark and DFlash2 on the Qwen GPU-resident FP8 path: MTP's 1.23x wall against the two width-8 drafters that lose to plain decode, and the per-row verify arithmetic that says why. |
| [Native C++ OpenAI concurrency validation](cpp_openai_concurrency_validation.md) | The end-to-end HTTP acceptance test and the vLLM head-to-head that followed it. |
| [Native C++ OpenAI tool-calling acceptance](cpp_openai_tool_acceptance.md) | Tool calling driven through a second turn, the `openai` SDK and `langchain-openai`, and the sidecar templating defect the first real two-turn request exposed. |
| [Ascend 910A attention and its measured ceilings](ascend_attention_optimization.md) | The Cube (`Mmad`) GQA attention operator, the three-way decode dispatch, the prefill phase table, and the bandwidth, all-reduce and `aclrtMemcpy` limits every remaining target has to clear. |
| [Ascend 910A TP collective overlap](ascend_tp_collective_overlap.md) | Why the overlapped all-reduce must slice by row count rather than a constant, the crossover sweep, and a device-synced profile at three prompt lengths showing the linear-attention recurrence is 45-56% of the stack against the collectives' 13-25%. |
| [Ascend decode: the all-reduce A/B, the event probe, and batch scaling](ascend_decode_collective_ab.md) | Why the decode all-reduce cannot be hidden on a second stream — seven measured orderings and the event probe that clears the primitive — and the batch table that reaches 114.5 TPS at TP4. |
| [Ascend 910A single-request decode](ascend_single_request_tps.md) | What the rows=1 step's 104.3 ms is made of — 101.2 ms of it batch-independent, 62 ms of that 129 all-reduces — the 85 TPS HBM ceiling, MTP measured as a 2.2x loss, and the hand-written cross-process collective built from the probe's payload-carried barrier: worth 106.33 -> 77.32 ms, 9.41 -> 12.94 TPS at rows=1 over thirty-six interleaved pairs, and shipped opt-in — the launcher's reproducibility gate, which both arms were failing (3 of 36 HCCL, 8 of 36 hand-written), turned out to be a workspace race elsewhere and passes 0 of 22 once that is fixed, so the gate is no longer the reason. Then the last push: the Cube's sixteen-row M tile filled from a decode step's single activation row by broadcasting it (a zero stride), for a further 13.05 -> 17.59 TPS over three interleaved pairs, on top of the hand-written collective rather than against it — and the two levers stacking on the merged tree to 39.4-39.8 ms and 25.15-25.37 TPS, still emitting the reference's tokens; and the device-side wait, withdrawn as incorrect on the strength of those same 10-of-10 gate failures, re-measured at 53.0-53.9 ms and 18.56-18.86 TPS at rows=1 with no replication and the reference's tokens. |
| [Ascend gated-delta value-axis slice](ascend_gated_delta_slice.md) | The measurement that shows the prefill recurrence is issue-bound rather than element-bound, why the cut is two slices and not four, the op-level and end-to-end A/B, and the run-to-run logit instability that rules the generated tokens out as evidence. |
| [The Ascend RoPE table's workspace slot](ascend_rope_table_workspace_aliasing.md) | Why the cos/sin table cannot share a pooled buffer with anything the stream is still executing, the lane evidence that identified it, and the three published findings — a reproducibility gate, a width-specific instability, and a 30% device-wait lever withdrawn as incorrect — that turn out to be the same defect, leaving single-request decode at 18.6 TPS with the reference's tokens. |
| [DeepSeek-V4 PersistentEngine serial baseline](deepseek_v4_serial_baseline.md) | The serial prefill/decode baseline for the native engine on DeepSeek-V4-Flash-0731, the environment ablation behind it, and why `--max-batch-size 8` currently changes nothing. |
| [Auditing the DeepSeek-V4.1-Flash shards from arrival to complete](deepseek_v4_1_shard_audit.md) | A header audit that distinguishes a missing shard from a wrong one, run at four points of the download: 44.0% of the checkpoint readable at 20 shards, four Engram checks still undecided at 46, and 39 of 39 passed with nothing undecided on all 48 — plus the tensor shapes and per-layer counts the shards assert. |
| [DeepSeek-V4.1-Flash: what the released checkpoint costs to run on one host](deepseek_v4_1_flash_host_run.md) | The byte census over all 96,085 tensors, the load report, what the tree occupies at TP4, the Engram gather's cold/warm/resident costs, what a generated token costs and what it is made of — 15 to 42 s on the host, 99.7% of it the fp4-to-bf16 expansion — and the measured PCIe and card-side rates that make the four cards the cheaper half of this model. |
| [DeepSeek-V4.1-Flash: the routed experts on the four cards](deepseek_v4_1_flash_device_experts.md) | The expert-parallel split over four 2080 Ti: the static 2/2/1/1 deal, the fp4 kernel's parity against the host expert, a measured 1.06–1.14 s per decode step phase by phase — then the dense tree cut across the same cards, one process per card, at 722–747 ms a step; and the honest negatives and caveats behind both: the four cards are worth 0.1–0.2 s of the expert step, because staging is host work and the largest term; the launch was four kernels serialized rather than one plus copies, worth 3.4× once the drain moved out of the card loop; and every figure is a warm-page-cache one, 19× worse on the same row once the cache is emptied — except with the resident bank attached, where the same emptied row is 782.9 ms against 17.01 s (21.7×) and warm is a wash, because `_stage` is a memcpy into the pinned arena whatever it reads from. And the per-layer resident pool, which is 4.0–4.2× on a prefill and **1.399× on a decode** once the decode is measured longer than one token — 765.0 against 547.0 ms a token over 256 steps at 600 rows, top-32 logits bit-identical, for 10.5 GiB of arena a card. On top of that, the batched expert call: the same pass resolved before it is staged and then issued a chunk at a time, **−27.0%** on a 512-token prefill at 288 pool rows with the logits unmoved, and the chunk rule that decides where a batch may not be cut. Plus what the pool broke: the two-buffer rotation advanced a row at a time, so with 72.5% of a pass's rows staging nothing it waited on slots whose copies were just issued — **6.89 s of a 30.35 s class wall** — and rotating only over rows that stage is bit-exact and drops that wait to 0.09–0.25 s, though the end-to-end A/B stays inside this host's noise, because the four ranks contend for one memory system and the floor is the bytes. |
| [DeepSeek-V4.1-Flash: what is left to optimize](deepseek_v4_1_flash_remaining_bottlenecks.md) | The pass after that configuration: `_stage` is on the critical path rather than thread-starved (198.5 ms in situ against 91.8 ms for the identical copies replayed) — but the overlap that price invited is **falsified**, 3–8× spent rather than 15% returned, because the replay's floor is a one-time pool warm-up; a decode-resident set is worth **1.399×** on a 256-step decode (765.0 against 547.0 ms a token, logits bit-identical) at 600 rows and 9.05 GiB a card above the step's own; `hc_mixes` has 208 µs a call inside no kernel; the prefill's floor is a layer's distinct experts, and `moe_multi_token_fp4_forward` is now wired in as `--expert-batched`: the staged rows are unchanged, but 512 rows a layer became 40 calls — one a layer, 512.0 rows a call — and **−27.0%** on a 512-token prefill (47.63 → 34.75 s a rank, 10.75 → 14.73 tok/s) with the top-32 logits bit-identical on all four ranks, plus the chunk rule that makes it safe and the 12/32, 4.39e-02 run that shows the rule is load-bearing; the per-op table's GB/s column reads ~4× high for any op the split cuts; and the levers that are already spent or already falsified. |
| [DeepSeek-V4.1-Flash: the decode step with the tree half inside a CUDA graph](deepseek_v4_1_flash_decode_graph.md) | Lever 6 of the page above, sized: the split graph (block halves captured, the expert call left eager because `_route_ids` synchronizes on the host) at a **frozen** `start_pos`, 833.6 → **561.6 ms** a step at 1024 and 843.8 → 566.1 at 1025 over forty of forty layers — 1.48–1.51×, all of it the tree's dispatch, with the logits **bit-identical** at both positions; the shared pool at **0.254 GiB, 6.5 MiB a layer**, 580× under the projection; why two frozen positions are needed (the compressor's fill and emit bodies, and `max diff = 0.000e+00` on both); the A-A control the ratio has to clear; and the pool-id trap that cost a 41-minute run — a `graph_pool_handle()` is a tuple with no lifetime, so a second capture round after the first round's graphs die asserts inside `create_or_incref_pool`. |

The currently authoritative per-model numbers live in the
[model guides](../models/README.md); pages under
[Archive](../archive/phase2-phase3/index.md) are historical records whose numbers
have been superseded.

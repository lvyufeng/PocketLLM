# Performance

Measured behaviour, reported separately for prefill and decode. Every page here is
a run record: hardware, checkpoint, commit, and invocation are stated with the
numbers. Read [Benchmarking and reporting rules](../guides/benchmarking.md) first
if you intend to compare any of them, and do not carry a decode figure to a
different generation length — a `TG4` measurement says nothing about `TG512`.

| Document | What it records |
| --- | --- |
| [DSpark speculative decoding with adaptive draft-length gating](dspark.md) | The DeepSeek-V4-Flash draft module, why the fixed accept threshold was falsified, and the online-calibrated gating that replaced it. |
| [FlashMemory 1M context](flashmemory_1m_context.md) | The FlashMemory + KV_SWAP design for long-context memory reduction, and its current implementation status. *(Chinese)* |
| [MiniMax-M2 decode bottleneck analysis](minimax_decode_bottleneck_analysis.md) | Per-phase profiling of MiniMax-M2 decode and prefill on TP4, and which phases the optimizations then targeted. |
| [Qwen4-Exp heterogeneous TP4 performance](qwen4_exp_performance.md) | Moving Qwen4-Exp experts to host memory: measured prefill gain, and why the real floor was the disk rather than PCIe. |
| [CPU offload and prefetch: the measured ceiling](cpu_offload_profile.md) | Layer-granularity offload measured rather than planned: the H2D and overlap ceilings, the storage rate behind them, and why #156's decode and prefetch targets are unreachable on this host. |
| [Qwen quantized KV cache at 65K (TG512)](qwen_kv_cache_65k_tg512.md) | The authoritative quantized-KV table at 65K/TG512, including corrections to two earlier throughput claims. |
| [Qwen drafter acceptance and speedup](qwen_drafter_acceptance.md) | The three-way head-to-head of MTP, DSpark and DFlash2 on the Qwen GPU-resident FP8 path: MTP's 1.23x wall against the two width-8 drafters that lose to plain decode, and the per-row verify arithmetic that says why. |
| [Native C++ OpenAI concurrency validation](cpp_openai_concurrency_validation.md) | The end-to-end HTTP acceptance test and the vLLM head-to-head that followed it. |
| [Native C++ OpenAI tool-calling acceptance](cpp_openai_tool_acceptance.md) | Tool calling driven through a second turn, the `openai` SDK and `langchain-openai`, and the sidecar templating defect the first real two-turn request exposed. |
| [Ascend 910A attention and its measured ceilings](ascend_attention_optimization.md) | The Cube (`Mmad`) GQA attention operator, the three-way decode dispatch, the prefill phase table, and the bandwidth, all-reduce and `aclrtMemcpy` limits every remaining target has to clear. |
| [Ascend 910A TP collective overlap](ascend_tp_collective_overlap.md) | Why the overlapped all-reduce must slice by row count rather than a constant, the crossover sweep, and a device-synced profile at three prompt lengths showing the linear-attention recurrence is 45-56% of the stack against the collectives' 13-25%. |
| [Ascend decode: the all-reduce A/B, the event probe, and batch scaling](ascend_decode_collective_ab.md) | Why the decode all-reduce cannot be hidden on a second stream — seven measured orderings and the event probe that clears the primitive — and the batch table that reaches 114.5 TPS at TP4. |
| [Ascend 910A single-request decode](ascend_single_request_tps.md) | What the rows=1 step's 104.3 ms is made of — 101.2 ms of it batch-independent, 62 ms of that 129 all-reduces — the 85 TPS HBM ceiling, MTP measured as a 2.2x loss, and the hand-written cross-process collective built from the probe's payload-carried barrier: worth 106.33 -> 77.32 ms, 9.41 -> 12.94 TPS at rows=1 over thirty-six interleaved pairs, and shipped opt-in — the launcher's reproducibility gate, which both arms were failing (3 of 36 HCCL, 8 of 36 hand-written), turned out to be a workspace race elsewhere and passes 0 of 22 once that is fixed, so the gate is no longer the reason. Then the last push: the Cube's sixteen-row M tile filled from a decode step's single activation row by broadcasting it (a zero stride), for a further 13.05 -> 17.59 TPS over three interleaved pairs, on top of the hand-written collective rather than against it — and the two levers stacking on the merged tree to 39.4-39.8 ms and 25.15-25.37 TPS, still emitting the reference's tokens; and the device-side wait, withdrawn as incorrect on the strength of those same 10-of-10 gate failures, re-measured at 53.0-53.9 ms and 18.56-18.86 TPS at rows=1 with no replication and the reference's tokens. |
| [Ascend gated-delta value-axis slice](ascend_gated_delta_slice.md) | The measurement that shows the prefill recurrence is issue-bound rather than element-bound, why the cut is two slices and not four, the op-level and end-to-end A/B, and the run-to-run logit instability that rules the generated tokens out as evidence — later attributed to a pooled workspace race rather than to the platform, and re-measured to zero spread in eight runs on the tree that fixes it. |
| [The Ascend RoPE table's workspace slot](ascend_rope_table_workspace_aliasing.md) | Why the cos/sin table cannot share a pooled buffer with anything the stream is still executing, the lane evidence that identified it, and the three published findings — a reproducibility gate, a width-specific instability, and a 30% device-wait lever withdrawn as incorrect — that turn out to be the same defect, leaving single-request decode at 18.6 TPS with the reference's tokens. |
| [DeepSeek-V4 PersistentEngine serial baseline](deepseek_v4_serial_baseline.md) | The serial prefill/decode baseline for the native engine on DeepSeek-V4-Flash-0731, the environment ablation behind it, and why `--max-batch-size 8` currently changes nothing. |
| [Auditing the DeepSeek-V4.1-Flash shards from arrival to complete](deepseek_v4_1_shard_audit.md) | A header audit that distinguishes a missing shard from a wrong one, run at four points of the download: 44.0% of the checkpoint readable at 20 shards, four Engram checks still undecided at 46, and 39 of 39 passed with nothing undecided on all 48 — plus the tensor shapes and per-layer counts the shards assert. |
| [DeepSeek-V4.1-Flash: what the released checkpoint costs to run on one host](deepseek_v4_1_flash_host_run.md) | The byte census over all 96,085 tensors, the load report, what the tree occupies at TP4, the Engram gather's cold/warm/resident costs, what a generated token costs and what it is made of — 15 to 42 s on the host, 99.7% of it the fp4-to-bf16 expansion — and the measured PCIe and card-side rates that make the four cards the cheaper half of this model. |
| [DeepSeek-V4.1-Flash: the routed experts on the four cards](deepseek_v4_1_flash_device_experts.md) | The expert-parallel split over four 2080 Ti: the static 2/2/1/1 `sorted` deal — the default until 2026-09-20 and now the opt-in one, against the `id` deal that replaced it, 1.49× on a 32768-token prefill and 1.42× on a 256K one — the fp4 kernel's parity against the host expert, a measured 1.06–1.14 s per decode step phase by phase — then the dense tree cut across the same cards, one process per card, at 722–747 ms a step; and the honest negatives and caveats behind both: the four cards are worth 0.1–0.2 s of the expert step, because staging is host work and the largest term; the launch was four kernels serialized rather than one plus copies, worth 3.4× once the drain moved out of the card loop; and every figure is a warm-page-cache one, 19× worse on the same row once the cache is emptied — except with the resident bank attached, where the same emptied row is 782.9 ms against 17.01 s (21.7×) and warm is a wash, because `_stage` is a memcpy into the pinned arena whatever it reads from — until that copy is deleted, which the closing section does and prices: registering the segment itself and reading it in place from `_upload` is **200/202 ms a decode token against 341–348 without the registration** (4.95–5.0 tokens/s against 2.87–2.93, 1.70–1.72×) and a 512-token prefill at **16.0 s a rank against 26.7–31.3 s**, with `_stage` 19.16 → 0.01 s of a 27.88 → 13.37 s class wall, `_take_buffer` exposed at 54.5–55.5% of what is left, all five logit dumps bit-identical at `max \|diff\| = 0.000e+00` over 64 × 129280 and `read_bytes` flat at 0, for one `cudaHostRegister` of 457.8 GiB — 59.6 s alone and 91.3–104.0 s with four ranks contending. And the per-layer resident pool, which is 4.0–4.2× on a prefill and **1.399× on a decode** once the decode is measured longer than one token — 765.0 against 547.0 ms a token over 256 steps at 600 rows, top-32 logits bit-identical, for 10.5 GiB of arena a card. On top of that, the batched expert call: the same pass resolved before it is staged and then issued a chunk at a time, **−27.0%** on a 512-token prefill at 288 pool rows with the logits unmoved, and the chunk rule that decides where a batch may not be cut. Plus what the pool broke: the two-buffer rotation advanced a row at a time, so with 72.5% of a pass's rows staging nothing it waited on slots whose copies were just issued — **6.89 s of a 30.35 s class wall** — and rotating only over rows that stage is bit-exact and drops that wait to 0.09–0.25 s, though the end-to-end A/B stays inside this host's noise, because the four ranks contend for one memory system and the floor is the bytes. And the deal: a card pays for the width of its staged set and not for its share of the rows, so dealing a drawing by `expert % world` instead of by its sorted position partitions the experts over the cards and balances it — **1.48x on a 32768-token prefill and 1.41x on the 256K one on the tree that ships** (1.22x and 1.19x on the kernels the A/B was taken on, before the three stacked prefill kernels landed under it), at **3.45x and 3.41x fewer staged rows** and the four ranks' worst-to-best spread 1.94x → 1.02x, for four more arena rows a card, **1.72 GiB more peak allocated on the light cards** and 0.40 / 0.04 GiB more reserved, and two deals that are not bit-identical, which is why `sorted` stayed the default for as long as it did. |
| [DeepSeek-V4.1-Flash: what is left to optimize](deepseek_v4_1_flash_remaining_bottlenecks.md) | The pass after that configuration: `_stage` is on the critical path rather than thread-starved (198.5 ms in situ against 91.8 ms for the identical copies replayed) — but the overlap that price invited is **falsified**, 3–8× spent rather than 15% returned, because the replay's floor is a one-time pool warm-up; a decode-resident set is worth **1.399×** on a 256-step decode (765.0 against 547.0 ms a token, logits bit-identical) at 600 rows and 9.05 GiB a card above the step's own; `hc_mixes` has 208 µs a call inside no kernel; the prefill's floor is a layer's distinct experts, and `moe_multi_token_fp4_forward` is now wired in as `--expert-batched`: the staged rows are unchanged, but 512 rows a layer became 40 calls — one a layer, 512.0 rows a call — and **−27.0%** on a 512-token prefill (47.63 → 34.75 s a rank, 10.75 → 14.73 tok/s) with the top-32 logits bit-identical on all four ranks, plus the chunk rule that makes it safe and the 12/32, 4.39e-02 run that shows the rule is load-bearing; the per-op table's GB/s column reads ~4× high for any op the split cuts; and the levers that are already spent or already falsified. |
| [DeepSeek-V4.1-Flash: the decode step with the tree half inside a CUDA graph](deepseek_v4_1_flash_decode_graph.md) | Lever 6 of the page above, sized: the split graph (block halves captured, the expert call left eager because `_route_ids` synchronizes on the host) at a **frozen** `start_pos`, 833.6 → **561.6 ms** a step at 1024 and 843.8 → 566.1 at 1025 over forty of forty layers — 1.48–1.51×, all of it the tree's dispatch, with the logits **bit-identical** at both positions; the shared pool at **0.254 GiB, 6.5 MiB a layer**, 580× under the projection; why two frozen positions are needed (the compressor's fill and emit bodies, and `max diff = 0.000e+00` on both); the A-A control the ratio has to clear; and the pool-id trap that cost a 41-minute run — a `graph_pool_handle()` is a tuple with no lifetime, so a second capture round after the first round's graphs die asserts inside `create_or_incref_pool`. |
| [DeepSeek-V4.1-Flash: a real graphed decode, token for token](deepseek_v4_1_flash_decode_graph_live.md) | The page above, un-frozen: the position now advances, `Pos` carries it through the six sites that are shaped by it, and `compress_len` becomes the cache's full width with the tail masked so a capture has one shape — 64 greedy tokens past a 1024-token prompt give **638/618 ms a token eager against 431/408 graphed**, 1.43–1.56× against a 3.2% A-A control, with **`max abs(logit diff) = 0.000e+00` on all 64 rows of a 64 × 129280 matrix**, 64/64 argmax, and `read_bytes` flat at 0; the split says where the rest is (**2.4 + 369.4 + 2.7 ms** of a 408 ms step — the eager expert call is 89%); and the plan's gate answered at node level: forty of forty layers dumped to DOT, the `[2, 8, 14]` dual-layer set derived rather than assumed (layer 20 has a compressor at ratio **1**), the **99-node** emit-minus-fill gap shown to be the row-production path rather than one op, and the compressed-KV quantizer's ATen family present in emit and absent from fill; residency at 15.28 GiB of 22 reserved with a 260.0 MiB pool. |
| [DeepSeek-V4.1-Flash: chunked prefill for a prompt too long for one forward](deepseek_v4_1_flash_chunked_prefill.md) | 256K context on four 2080 Ti, and what it costs: `Backbone.forward(..., chunk=n)` splits a prompt into `n`-token forwards that compose because every layer already keeps its own state — **262144 tokens in 3711.0 s, 70.6 tok/s on all four ranks, 57.98 s a 4096-token chunk, 17.34 GiB peak** of the 22000 the card reports; the five caches a boundary has to carry and the ten-width `torch.equal` suite that holds them, with `_is_continuation` keyed on the query count rather than the position because that is what the decode graph captures; the width ceiling at **40 MiB a 1024 tokens** of Hyper-Connections residual (8192 fits, 16384 does not) with the width curve measured from below — **18.57, 15.52 and 13.89 ms a token at chunks of 1024, 2048 and 4096**, which two constants reproduce to 0.01 ms as **~10.4 ms of compute a token plus ~1.47 ms a staged row**, a row being 17.93 MiB at 12.8 GB/s of a 15.75 GB/s PCIe 3.0 x16 link, and the same slope is what the 148→288 pool A/B lands on (2.37 → 1.71 rows a token for 4.07 s a chunk, 1.51 ms a row); and the pool width at 256K — **148 rows and not 288**, since 288 is −7.2% a chunk at 32768 for +4.34 GiB and dies in the second chunk at 262144, 2.45 GiB of that being the arena's 140 rows at 17.93 MiB; and a 22-tap phase table over one 4096-token chunk at 32768 — **99.0%** of its 65.54 s accounted for on all four ranks, with **73.5% of the quiet 56.96 s chunk in the MoE**: a 32% grouped fp4 GEMM over four calls a layer, a 24–29% expert H2D at 1.75 ms a row and 166.5 GiB at 10.0 GB/s, a 14% sparse-attention score pass at 201 ms a layer, and the host row bookkeeping that its 12–17% band turned out not to be — **1.52 s of the chunk's 163,840 `_resolve_row` calls, 9.3 µs each**, the rest of that 5.22 s row being the probe's own barriers, on the same call count in the warm-up and on a tree with three further kernel changes merged in, measured by re-running the profile with each tap's postamble barrier split out of its number — and the Hyper-Connections ceiling the chunk width runs into costing **2.9%** of a chunk; plus what a boundary does and does not reproduce — the five caches exactly, the logits not — with the first `1e-6` difference localized to layer 0's gate and routed experts, the two modules whose GEMM row count changed, and the one-shot arm re-run bit-identical at all ten taps as the control. The 256K leg's 3711.0 s and 70.6 tok/s are the `sorted` deal; the `id` deal is the same leg at **3121.0 s and 83.99 tok/s** (1.19x), 0.53 staged rows a token against 2.31, at 17.72 GiB a card against 17.34 — [priced on the expert page](deepseek_v4_1_flash_device_experts.md#the-deal-is-a-choice-and-dealing-ids-instead-of-positions-balances-the-staged-set) — and on the tree that ships, which is this one plus the three prefill kernels, the same pair is **2532.75 → 1789.92 s and 103.50 → 146.46 tok/s (1.41x)** at 0.53 staged rows a token against 2.35. It also takes apart **the one row of a chunk that grows with context**: `attn.compress_kv` — with `attn.indexer` nested inside it rather than beside it, 99% of its parent on the eight calls that reach it — is **7.7% of a 4096-token chunk at 32768 against 16.4% of one at 262144**, and on that span the contained `attn.indexer` alone goes 2.08 → 5.11 s while the chunk moves 27.39 → 31.34 s on the shipping tree (the 56.96 s quiet chunk above is the pre-kernel branch, its `attn.sparse` being 8.05 s against this one's 2.05) and every other row of the two phase tables is flat or lower — `attn.indexer`'s 1072 tile events are 96% the candidate path, whose count is context-invariant because its keys are 16384 gathered positions whatever the prompt is, so the growth is inside the prefix tile or in a per-call cost that scales with the width, and the row is **90% neither its tile count nor its arithmetic** (four arms, the three that move at most 2.8% of the tiles buying 1%, the one that reaches all of them cutting them 3.5× for 0.90×) — with its five levers named against their gates: the `all_reduce` that upcasts to fp32 and casts back to bf16 anyway, so a prefix tile puts **33.6 MB on the wire for the 16.8 MB tensor it carries**, gated on the picked ids; a one-tile lookahead over that collective, which has no numerics in it at all; the `INDEXER_CAND_TILE` retile, worth 0.90x on the row at 32768 and 3.9% of it at 262144 because the candidate path does not grow with the width; the candidate stream's own early-out, a device→host read on every push that is worth one merge and ships off; and then the fifth, which **removes the collective instead of paying or hiding it** — sharding the indexer by query rows folds the query and head axes into the GEMM's M dimension so each rank's score is complete for its own rows and no `all_reduce` is needed at any tile, at the price of a replicated `wq_b` and one all-gather of the picked ids (~67 MB a chunk over the eight sources, ~12 ms). One process an arm, the same probe on both: **prefix 0.415x and candidates 0.244x at 32768, 0.395x and 0.243x at 262144**, and the wall **210.4 → 199.9 s (0.950x)** there against **1808.1 → 1652.9 s (0.914x)** on the 256K leg, a 4096-token chunk going 28.25 → 25.83 s, with the greedy chain identical **32 of 32 tokens at 32768 and 96 of 96 at 262144** under a per-step bound of each step's `\|Δlogits\|` against the smaller top-2 margin (worst ratio 0.330). |
| [DeepSeek-V4.1-Flash: the three prefill optimizations, stacked](deepseek_v4_1_flash_prefill_stack.md) | The composition the three passes that followed that page cannot show one at a time: one arm on the 256K branch and one on the same branch with all three merged, the identical 22-tap probe at the identical geometry, all four ranks — **65.94 → 48.48 s instrumented and 56.87 → 38.07 s with the taps off (−33.1%), against the three changes' own deltas of 5.33, 8.98 and 4.58 s summing to 18.89 for an 18.80 s measurement**, five parts in a thousand, which is what says the two changes that share a launcher and two symbols are still independent in the composition; where the seconds are now — the expert H2D at **15.02 s and 39% of the quiet chunk**, with the staged-row count rather than the call count as the lever, the TP combine at the end of `MoE.forward` visible at 2.95 s (73.8 ms a layer, 8%) because the sparse pass is down to a fifth of what it was, and then separated from the attention's `wo_b` combine — the same `make_all_reduce` of the same `[4096, 5120]` activation, so the same **80.00 MiB fp32** a layer, merged into one 80-call row at 52.28 ms a call until the probe keyed its scopes by call chain — which shows them 5.4× to 26× apart: the attention site is **13.2–19.8 ms a call on all four ranks**, the isolated rate probe's **13.58 ms at 5.75 GiB/s** for that message, while the MoE site is **74–86 ms a layer on the ranks dealt 2 of the 6 sorted expert slots and 347–352 ms on the ranks dealt 1**, inverted against the routed path those ranks run (17.163/17.395 s against 9.362/9.262), so that row is a rendezvous with the straggler and not a bandwidth floor — which is why a bf16 reduce, the obvious move against an fp32-widened 80 MiB message, would be worth **0.7% of a chunk** and nothing off the row that carries the seconds; and the host row loop re-measured as the **1.52 s over 163,840 calls** it is rather than the 6.7–9.5 s band the barriers in its tap made it look, on both trees and in every warm-up chunk; plus the caveat that the two arms are two trees run in turn, whose extensions were rebuilt after their runs, so the tree and the rows the changes do not touch rather than a binary md5 are the control — and the parity behind it: the stacked tree run twice is **bit-identical at all nine dump positions on all four ranks** (`max \|diff\| = 0.000e+00`), so the base-against-stacked difference is the three kernels and no host noise, and it leaves the argmax of every position where the base tree put it, against a top-2 gap 2.7× to 6.0× the worst perturbation. Then the same two arms over a full 262144-token prompt, where the composition is worth the same thing: **3722.8 → 2520.7 s and 70.4 → 104.0 tokens a second, a 4096-token chunk 58.17 → 39.39 s (1.477×)**, flat between 1.479× on the chunk at the 32768 boundary and 1.476× on the last one, with the stacked arm staging 0.8–1.8% *more* expert rows than the base arm and both arms growing by the same 5.2 s across the leg — the prefix-dependent 13% of a 256K chunk that the three changes leave alone. That 256K leg is the `sorted` deal — 2.31-2.35 staged expert rows a token on the ranks dealt two of a row's six sorted slots against 1.23-1.24 on the two dealt one — and the id deal, which partitions the experts over the cards and so removes that imbalance, is priced on the [expert page](deepseek_v4_1_flash_device_experts.md#the-deal-is-a-choice-and-dealing-ids-instead-of-positions-balances-the-staged-set) at **1.19x on this same length** (3121.0 s and 83.99 tok/s against 3701.0 and 70.83), on the tree without these three kernels — and re-taken on the tree that ships, where these three kernels *are* the shipping tree: **2532.75 → 1789.92 s and 103.50 → 146.46 tok/s (1.41×)**, whose `sorted` arm is this page's stacked arm read again to 0.5%, so **1.477x and 1.415x make 2.080 against 2.080 measured** — 3722.8 s to 1789.92 s end to end. |
| [DeepSeek-V4.1-Flash: the batched MoE call's weight reads, coalesced](deepseek_v4_1_flash_moe_weight_staging.md) | The two kernels that read the checkpoint's per-expert weights inside a batched prefill chunk, priced standalone and then in the model: the per-expert `[inter_dim][blocks_k][16]` layout leaves consecutive columns `dim / 2` bytes apart, so a warp's 32 lanes take 32 separate 32-byte sectors for 16 useful bytes each; staging a K strip of each weight matrix through shared — `tile=32`, `strip=4`, 21 KiB a block, 3 blocks a multiprocessor — measures **3.89×** off the model — **1.66×** from widening the token tile from 8 rows to 32 and **2.34×** from the coalescing — against a transposed arena's 5.88×, which would need a one-time rewrite of the bank, and pays **3.06×** on `moe_multi_w1w3_fp4_kernel` and **3.14×** on `moe_multi_w2_partial_fp4_kernel` in the model, for a 4096-token chunk at 32768 of **56.49 → 47.51 s (72.5 → 86.2 tok/s)** with the seven device-table rows the change does not touch moving at most 3%; and the 69.24× CSR for the reduction, bit-identical and recorded there as the follow-up the [next page](deepseek_v4_1_flash_moe_reduce_csr.md) ships. |
| [DeepSeek-V4.1-Flash: the batched MoE call's reduction, grouped per token](deepseek_v4_1_flash_moe_reduce_csr.md) | The follow-up the page above named, and the correction its call sites forced: the reduction is `Θ(tokens × pairs)` — 11.3 G compares a call at the prefill geometry, of which the 2100 that find a pair are one in `tokens`, 0.095% — so the pairs are grouped by token into a CSR and the loop runs over the row's own; the grouping is built **on the device, inside the launcher**, from three ATen ops, because one call site resolves its routing with tensor ops and reading it back would be the D2H sync that path exists to avoid; a stable sort is what makes the two paths bit-identical rather than nearly so, `kCsrMinWork` is why a speculative-verify call still takes the scan, and `DEEPSEEK_MOE_CSR_REDUCE` is the A/B knob. |
| [DeepSeek-V4.1-Flash: what one request costs, through the launcher](deepseek_v4_1_flash_single_request_capability.md) | The launcher's own answer to the two columns the [device experts page](deepseek_v4_1_flash_device_experts.md) could only take with the probe: prefill read as `prompt_tokens / (elapsed − decode_seconds)` off the two walls the CLI prints, four prompt lengths, one leg a process — **105.98 tok/s prefill at 32716 tokens and 103.58 at 262865** (default `sorted` deal, 148 pooled rows, 4096-token chunks) against the 1024-token forward's 55.0; **decode 4.98 tok/s at 1024, 4.37 at 32768 and 3.86 at 262144** graphed against 2.82/2.75 eager, so 5 tokens a second is a claim about a short context and not a guarantee; the eight 1024-token legs' continuations identical to the byte across two trees (`md5 d1775d3580129c2953d16060b73c915c`, 301 B of 64 tokens); a graphed step's split stable at ~92% eager expert call (1.3 + 184.4 + 1.0 ms of 201 at 1024, 2.1 + 239.6 + 1.2 of 259 at 262144); and one leg that measured no decode at all because its prompt ended mid-clause and the first token came back eos. It also now records what these rates are not: `deepseek_v41` has no registered engine in `cpp_engine` and no entry in pocketllm's engine map, its dimensions sit under `text_config` where `ModelConfig::from_hf_config` does not read, and `engram` appears nowhere under `cpp_engine/` — so at the time these legs were taken the launcher was the only path that ran this checkpoint; the server's own path and numbers are [on the served gate page](deepseek_v4_1_flash_served_gate.md). |
| [DeepSeek-V4.1-Flash: the same checkpoint behind the server](deepseek_v4_1_flash_served_gate.md) | The launcher's configuration behind `pocketllm serve --backend v41`, four ranks one process a card, and the two columns the deployment was gated on: **137.5, 140.8 and 138.3 tok/s of prefill at a 1364-token prompt over three back-to-back requests at 4.50, 4.45 and 4.53 tok/s of decode**, against the first request's 108.0 and 4.53, which pays the capture pass in its ttft — and all four requests returning, which is the point, because the first cut of the adapter returned only the first one. **The defect only a service can have**, written out: `graphs=True` leaves a recording on every block and the caller is the one that releases it, `Block.forward` hands *every* forward to `block.decode_graph` whatever its width, and the sink that recording writes into was sized one row by its first real pass — so the next 1364-token prompt died inside the previous request's graph with `output with shape [1, 1, 4, 5120] doesn't match the broadcast shape [1, 1364, 4, 5120]`, fixed at both ends of the responsibility: the adapter releases `generation.driver` when a request ends and `_decode_graphs` releases its own if a callback unwinds out of it (59 tests in `tests/test_v41_backend.py`, 5 in `tests/test_models_deepseek_v4_1_generate.py`). At the reference's 32768 leg the two columns go opposite ways and the deal is not why: with `expert_deal=sorted` held fixed at the reference's own setting, **142.4 and 139.6 tok/s of prefill against the launcher's 105.98** and **3.91 and 3.85 tok/s of decode against 4.37** — the server's prefill a third faster and its decode a tenth slower, 244.8–249.6 s of wall against 323.3 s for a prompt of the same length — while the deal itself is worth **1.12× on the server's own prefill** (159.0 and 158.5 on the shipping `id` deal against those 142.4 and 139.6) **and nothing on its decode** (3.82–3.95 against 3.85–3.91, a wash), which is not the 1.48× the launcher measured for the same deal and is the reason the launcher's deal figure does not carry over to this path; the decode gap is mostly the ~20–30 ms a step that timing the SSE cadence at the client costs against the launcher's in-process `decode_seconds` (253–262 ms a step against 229, 220–225 against 201 at the short leg), and the prefill one is a client-side ttft that is a floor rather than a rate, and it is not the kernel work either — the launcher's rows at `f7572f0` are already past the three stacked prefill kernels (#296/#297/#298) and predate only the `id`/`sorted` deal flip and the indexer row split, with the deal held fixed at `sorted` on the compared arm. Plus the per-request re-capture the release makes necessary (2.7 s of the first request's ttft), what the adapter refuses to do (`supports_batch=False`, no prefix caching, no logprobs, cancellation a per-step collective that cannot interrupt a prompt's forward), the over-length refusal that arrives **in-band on the stream** 0.10 s after the role chunk so a client counting tokens reports "no tokens" rather than an error, and what the page does not claim — the 262144 leg has not been served, the two columns are not reconciled to a single number, and the acceptance is the gate rather than a token comparison. |
| [Serving latency baseline (vLLM convention)](serving_latency_baseline.md) | The first client-side record: TTFT, TPOT, ITL, E2EL and throughput for 24 streaming chat requests at 2 req/s on four 910B, the measured role-chunk cost (1013.26 ms between the two TTFT latching rules), the client and engine agreeing on the per-token time, and the earlier smoke whose 15.78 ms a token is faster than this checkpoint's fastest measured decode step. |
| [Serving latency with IPC all-reduce and device-side wait](serving_latency_optimized.md) | The same workload with `POCKET_ASCEND_IPC_ALLREDUCE` and its device-side wait on, both opt-in and neither shipped: mean TPOT 151.30 → 99.49 ms (-34.2%), per-stream decode 6.61 → 10.05 tok/s, 47.28 → 70.29 tok/s aggregate, and the ladder that attributes it — 103.8 ms at the shipped collective, 53.2-53.6 ms with the two switches, 39.4-39.8 ms only once the Cube's row replication is added. |
| [Serving concurrency, throughput and the TFLOPS behind them](serving_throughput_scaling.md) | How many streams the engine actually runs at once, and what each step is worth: the ladder from 1 to 112 slots, the 96-slot knee where row-steps/s peaks at 203.82 for 10.445 TFLOP/s (1.18% of the four cards' 884.7), the KV pool that kills a rank silently at 120 slots while `--max-batch-size` itself is fine, the bimodal TTFT that makes the whole wave's prefill one blocking serial cost, the ~204 ms of that cost every prefill call pays before its first token-dependent FLOP and the ~2x a merged forward would recover, and the roofline that says both curves are bounded by that batching defect rather than by bandwidth. |

The currently authoritative per-model numbers live in the
[model guides](../models/README.md); pages under
[Archive](../archive/phase2-phase3/index.md) are historical records whose numbers
have been superseded.
